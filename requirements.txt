# Pinned from the dev environment (Python 3.11, RTX 4050, CUDA 12.1).
# Teammates WITHOUT an NVIDIA GPU: install the CPU torch wheel instead:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# Everything else below works identically on CPU (XGBoost falls back automatically).
pandas==3.0.5
numpy==2.4.6
scikit-learn==1.9.0
scipy==1.17.1
statsmodels==0.15.0
xgboost==3.2.0
torch==2.5.1
river==0.26.1
fastapi==0.138.0
uvicorn==0.49.0
pydantic==2.13.4
matplotlib==3.11.1
joblib==1.5.3
httpx==0.28.1
pytest==9.1.1
