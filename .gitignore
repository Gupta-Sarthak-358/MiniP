__pycache__/
*.pyc
.pytest_cache/
.venv/
# Big training artifacts: raw files never committed (GitHub blocks >100MB).
# Shipped instead as split zips (<100MB each), loaded transparently by predict.py.
models/*.pkl
models/*.pt
!models/*.zip
# Regenerated by backend/simulator/generate_dataset.py (~40 s).
data/historical.csv
!data/*.zip
*.db
